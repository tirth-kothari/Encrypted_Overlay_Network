## Setting up the client

```
$ cd activities/activity1/starter/client
$ python3 -m pip install --upgrade pip
$ python3 -m pip install pipenv
$ python3 -m pipenv shell

(client) $ pipenv install
(client) $ pipenv install --dev
```
